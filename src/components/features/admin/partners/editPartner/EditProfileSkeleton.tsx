import Spinner from "@/src/components/basics/Spinner";

export default async function EditProfileServer() {
  return (
    <div className="flex items-center justify-center h-[1000px]">
      <Spinner />
    </div>
  );
}
